trigger:
  branches:
    include:
      - release/shopping/v*
      - refs/release/shopping/v*

pool:
  vmImage: "ubuntu-latest"

variables:
  azureServiceConnection: "azure-rm-andaha-dev-connection"
  containerRegistryConnection: "andaha-container-reg-connection"
  imageTag: "$(Build.SourceBranchName)"
  bicepTemplateFile: "src/Services/Shopping/pipeline/bicep/shopping-main.bicep"

name: "$(Build.SourceBranchName)"

stages:
- stage: build
  displayName: Build
  jobs:
    - job: build_and_push_image
      steps:
      - task: Docker@2
        displayName: build image
        inputs:
          command: build
          arguments: '--build-arg PatPlaceholder=$(NugetFeedAccessToken)'
          dockerfile: src/Services/Shopping/Andaha.Services.Shopping/Dockerfile
          repository: andaha/services/shopping
          containerRegistry: $(containerRegistryConnection)
          tags: |
            $(imageTag)
            latest
          
      - task: Docker@2
        displayName: push image
        inputs:
          command: push
          dockerfile: src/Services/Shopping/Andaha.Services.Shopping/Dockerfile
          repository: andaha/services/shopping
          containerRegistry: $(containerRegistryConnection)
          tags: |
            $(imageTag)
            latest

      - task: PublishBuildArtifacts@1
        displayName: "Publish Artifact"        

- stage: deploy_dev
  displayName: Deploy to dev
  dependsOn: build
  jobs:
  - job: deployment
    steps:
    - task: AzureCLI@2
      displayName: 'Deploy to dev'
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          acrUser=$(az acr credential show --name andaha --resource-group andaha --query username -o tsv)
          acrPassword=$(az acr credential show --name andaha --resource-group andaha --query "passwords[0].value" -o tsv)
          az deployment group create --resource-group andaha-dev --template-file $(bicepTemplateFile) --parameters stage="dev" containerRegistryUsername="$acrUser" containerRegistryPassword="$acrPassword" version="$(Build.SourceBranchName)"

- stage: deploy_prod
  displayName: Deploy to prod
  dependsOn: deploy_dev
  condition: succeeded()
  jobs:
  - deployment: deploy_prod
    displayName: Deploy to production
    environment: Production
  - job: deployment
    steps:
    - task: Docker@2
      displayName: build image
      inputs:
        command: build
        arguments: '--build-arg PatPlaceholder=$(NugetFeedAccessToken)'
        dockerfile: src/Services/Shopping/Andaha.Services.Shopping/Dockerfile
        repository: prod/andaha/services/shopping
        containerRegistry: $(containerRegistryConnection)
        tags: |
          $(imageTag)
          latest
        
    - task: Docker@2
      displayName: push image
      inputs:
        command: push
        dockerfile: src/Services/Shopping/Andaha.Services.Shopping/Dockerfile
        repository: prod/andaha/services/shopping
        containerRegistry: $(containerRegistryConnection)
        tags: |
          $(imageTag)
          latest

    - task: AzureCLI@2
      displayName: 'Deploy to prod'
      inputs:
        azureSubscription: $(azureServiceConnection)
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          acrUser=$(az acr credential show --name andaha --resource-group andaha --query username -o tsv)
          acrPassword=$(az acr credential show --name andaha --resource-group andaha --query "passwords[0].value" -o tsv)
          az deployment group create --resource-group andaha-prod --template-file $(bicepTemplateFile) --parameters stage="prod" containerRegistryUsername="$acrUser" containerRegistryPassword="$acrPassword" version="$(Build.SourceBranchName)"
